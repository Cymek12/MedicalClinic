pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                // Pobranie kodu z repozytorium
                echo "Building the application..."
            }
        }
        stage('test') {
            steps {
                // Budowanie projektu Mavenem
                echo "Testing the application..."
            }
        }
        stage('deploy') {
            steps {
                // Uruchomienie testów
                echo "Deploying the application..."
            }
        }
    }
}
